# 改动总结：资产库架构重构（无模式链路 + 三库面板 + 转码内联）

分支：`experimental-branch-2`

## 一句话

项目文件夹收敛为 **assets / finals / latent / texts 四库**（`manifest-v2.2`，旧裸名双路径兼容）；
删除手动模式选择，链路由后端按实际引用自动推导（证据：官方无 UNET 校验、单 DiT 架构）；
资产库成为纯库（总量不限、打标、入库拷贝、行内剪辑/转码）；引用分书写/调度/锚定三层归位；
转码从镜像节点改为**主节点内联短路执行**；修掉转码卡死与本轮报障的一批 bug。

## 1. 项目四库规范（`checkpoint.py` / `projects.py`）

- `ensure_project_dirs` / `resolve_project_file`：新产物进 `finals/`（段/成片/合并/缩略图）、
  `latent/`、`texts/（prompts_latest.json）`；旧根目录裸名双路径兼容读，不主动搬家。
- `MANIFEST_SCHEMA → v2.2`；`create_project` / `ckpt_dir` 自动建四目录。
- `trim_asset` 产物落源文件同库；`merge_project` / `_autosave_final` / 二采成片全部收进 `finals/`；
  `upscale_files` 改 `finals/` 前缀，`_record_valid` / `try_final` 双路径兼容。
- 主循环 manifest 全量覆写携带三库键（修了旧的“跑一次链冲掉 assets/latents/clips”丢键 bug）。
- `delete_file` 路由放行四级路径（`h3_projects/<项目>/<库>/<文件>`）。

## 2. 三库 ops 与互斥（`projects.py` / `routes.py` / `checkpoint.py`）

- 新增 `move_media`（assets↔finals 搬家 + manifest 全表改写不断链 + 可选登记标签）、
  `split_av`（画面 mp4 + 音频 wav）、`import_asset`（input 拷贝进 `assets/`，重名加后缀，登记 manifest）、
  `slice_latent kind=av/video/audio` 分开存。
- 生成互斥锁：`checkpoint.mark/unmark/is_busy`，主节点 `execute` 外壳置位，
  trim/slice/move/split 遇忙回 423，前端置灰 + 横幅（`GET /h3chain/busy`、`H3Latent.isBusy`）。
- 新增 `POST /h3chain/probe`（只读元数据：fps/frames/duration/尺寸/有无音轨）与 `media.probe_media`，
  供裁剪子面板填帧窗；`DELETE`/`merge` 等长耗时仍走线程池。
- `POST /h3chain/import_asset` 不受生成锁影响（纯文件拷贝，随时可用）。

## 3. 无模式链路自动推导（`nodes.py`）

- **证据**（已查官方实现）：`ImageToVideo` / `ReferenceToVideo` 只是 conditioning 构造器，
  零模型校验；采样 DiT 是同一套 `MiniMaxH3Model`，`supported_models` 只有一个 `MiniMaxH3`
  配置——fl2va/ref2va 之分只在权重文件。fl2va 跑多参机械可跑，效果看权重。
- 删除全部 5 条模式互斥报错；`生成模式` 控件留占位不再读取（旧工作流兼容）。
  有有效引用即走 ref conditioning；首帧可与引用共存（头锚 keyframe 叠加）。
  均匀链 `chain` 指纹与旧版逐字一致，旧项目续跑不断档。
- 缺省引用 = 只用提示词文本 `[[标签]]` 出现的素材（库再大也不逼勾选）；
  素材按段按需加载（引用并集，每标签全链解码一次；未引用文件不碰不占显存），
  报告注明跳过数；缺文件只对真引用它的段早爆点名。
- 资产 `roles: [首帧图/尾帧图]`：各只留一张（多标报错点名），仅图片可标；
  链首/链尾锚来源 = 标注资产（旧三槽位有值则优先，旧链零变化）。
- 段 `tail_src: {asset} | {latent}` 统一尾锚（替代旧全局每段尾帧锚定；FL2VA 终点段仍优先尾帧图），
  进段哈希；旧 `ds.last_frame` 兼容回退。
- `latent_ref.src` 外源桥保留（形状校验失败回落上段尾并注记）。

## 4. 前端（`web/h3_director.js` 等）

- 左栏素材池面板删除，并入三库资产页；模式条删除（链路页改为只读自动推导说明）。
- 右栏：📐基础设置 + 🔗视频延续（📌关键帧/🩺检测重摇两子折叠）；段卡设置页双按钮
  （自动引用上段/自动按序生成）+ 本段 latent 引用/保存 + `tail_src` 选择器。
- 统一引用条（tab 外常驻）：chip 亮绿=本段已调度，点按切换（可取消）；
  主框下打开调度顺手补 `[[标签]]`，具象化下同步加参考条目，标注徽标与资产库一致。
- 调度 chips（含 933 计数）迁入具象化参考组“分段素材调度”；`defaultV2Mode` 改按实际引用判定。
- 资产页：roles 开关（仅图片行）、上传即入库、旧池文件一键入库、旧三槽一键导入打标、
  裁剪/转码行内子面板（帧窗/秒窗+分支）、转码任务队列区（加入/移除/清理/清空/中断队列）、
  本库剪辑片段区；成片页 clips 按库分区、wav 行 audio 播放、操作转圈不锁面板。
- `ds.ref_assets` 与项目 `manifest.assets` 双向绑定：切项目即换池；删卡/改名/改标自动落盘。
- `getDs` 透传 `roles` / `tail_src`；模板说明同步去模式化。

## 5. 转码卡死修复 + 内联（`latent_tools.py` / `media.py` / `nodes.py`）

- 根因：整文件全解码进内存 + 整窗单次 VAE 前向（H3 训练长度 124–362 帧）+ 零进度。
- `media.decode_av(path, start_f, end_f, fps)`：深窗口 seek + 帧 pts 精确定位 + 到窗末早停；
  旧全量调用兼容不变（桩验证：起止精确、无 seek 误跳）。
- `MAX_ENCODE_FRAMES = 362`：`H3MediaToLatent` / `H3LatentExtract` 超限直接报错指引缩窗；
  解码本身也有界（最多读上限+1 帧，到片尾模式不再全片进内存）；四步进度条 + 终端打印。
- `H3LatentUpscale`：源超 720 帧拒收 + 进度打印。
- 镜像节点方案废弃：`ds.transcode_jobs` + 主节点 `_execute_transcode_only` 短路专跑
  （只跑转码不跑链不耗种子，任务间隙响应队列中断，产物登记 latent 库，前端凭证据自动清理）。
  `run_transcode_job` 纯函数供节点与主节点共用；模板镜像节点已移除（两模板恢复 32 节点/33 边）。

## 6. 本轮报障修复对照

- 素材「`assets/seg_000 (1).mp4`」报缺失：`asset_check` 只查 input → 现按项目解析
  （`assetCheck` 传 dir），文案区分项目文件夹/input 目录；空格括号文件名本就合法。
- 分段默认绿 + 不能取消：chips 改调度态显示 + 点按切换。
- 资产库跨项目共享：切项目绑定 `manifest.assets` + 改动自动落盘。
- 首尾帧标注出现在音视频行：仅图片行可标 + 后端 kind 校验。
- 分离音频去向：本库 clips 区 + audio 播放器 + op 徽标。
- 裁剪去成片库：产物落源文件同库 + clips 按库分区展示。

## 7. 接口变更表

- 新增节点：`H3MediaToLatent`（库转存档）、`H3LatentUpscale`（库内放大+可选重采）。
- 新增路由：`GET /h3chain/busy`，`POST /h3chain/move_media|split_av|import_asset|probe`。
- ds 新增：`ref_assets[].roles`、`segments[].tail_src`、`transcode_jobs`。
- manifest：`assets[]` 增 `roles`，`latents[]` 增 `kind/upscale/resample`，`clips[]` 增 `op`。

## 8. 验证

- `pytest tests/test_rework_v2.py`：25/25（新增 roles 透传、`tail_src`/`latent_ref.src` 透存、
  `import_asset` roundtrip、路由挂载断言、`transcode_jobs` 归一化断言）。
- 专项桩验证（本机无 av/GPU，仅逻辑层）：分窗解码起止精确、转码上限先于解码触发、
  内联短路（项目不一致跳过/无项目名/任务间中断）、1200 条入库不限量、单段 10 图仍拒绝。
- `node --check` 全前端文件通过；两模板链接完整性校验通过。
- 待 Comfy 实测：混排链、标注冲突报错、转码队列全流程、旧档续跑、fingerprint 目录 + 项目资产组合。

## 9. 补丁：latent 外源桥打通 + 转码队列归一化

- **bug**：`seg_latent_ref` 构造丢了 `src` 字段，`_inject_guide` 的 latent 外源分支
  实际不可达（外源注入只有 `tail_src.latent` 一条活路）。已补透传，
  与 `projects._clean_seg_field` 同口径（`latent/<名>.pt` + 文件名合法性检查，
  文件存在性仍由 `_load_library_latent` 在执行期校验）。
- **robustness**：`getDs` 加 `transcode_jobs` 白名单归一化（必填 `src`/`save_name`，
  数值钳位、分支白名单、`status` 强制 `queued`、上限 32 条），畸形条目不再常驻 widget。

## 10. P0 资产存储双层 + 编译期子集解析（`asset_store.py` / `routes.py`）

- 新增 `asset_store.py`（stdlib only、无 torch、import 期零副作用）：
  全局库 `user/minimax_h3/library/{images,videos,audios}` 内容寻址
  （`register_content` sha256 命名 + 秒传，manifest schema `h3/asset-library-v1`，
  抄 Guide Reference Sheets）；项目侧 `manifest["asset_links"] [{asset_id, alias}]`，
  alias 复用旧 label 命名空间；旧 `manifest["assets"]` 经 `migrate_legacy_assets`
 （`sha1(label|kind|file)` 稳定 ID，首个为准）零改动可读。
- `compile_refs / compile_segments`：alias 与 asset_id 双解析，按类型独立编号
  `<Picture>/<Video>/<Audio>`（与 `nodes._kind_tokens` 同规则），单段 9/3/3 与
  未知引用沿用 `E_MEDIA_LIMIT` / `E_REF_UNKNOWN`；dict 引用 `{asset, use}` 透存角色。
- 新增 `POST /h3chain/compile_refs`（根 + `/api` 双挂载）：`{dir?, assets?,
  asset_links?, global_assets?, refs?|segments?}` 干跑编译，前端切段即时显示
  真实 tag，不用排队才知道错。单测 `tests/test_asset_store_p0.py` 10 项。

## 11. P1 后台转码队列 + 上传即入库（`transcode_queue.py` / `routes.py`）

- 新增 `transcode_queue.py`（进程内 job 表，与 `checkpoint._BUSY` 同口径）：
  `submit/claim/begin/progress/complete/fail/cancel/finish_cancelled`，
  状态机 queued→running→done|error|cancelled；running 取消是协作式
  （`cancel_requested` + `interrupted_checker` 给 `run_transcode_job` 的
  `_interrupted` 回调）。VAE 仍只在队列执行内拿（架构约束），“后台”指登记/
  轮询/取消脱离画布连线与 `ds.transcode_jobs` 手写。
- 新路由（均不受 423 生成锁影响）：`POST /h3chain/transcode_submit`
  （项目/源文件存在性预检 + 形状校验），`GET /h3chain/transcode_jobs|job`
  （轮询，`?dir/?id`），`POST /h3chain/transcode_cancel`，
  `POST /h3chain/library_upload`（multipart 拖拽 / JSON `{src}` 双模式，
  扩展名-kind 校验，`link_dir+alias` 可选直链项目，409 复用乐观锁）。
- `projects.link_asset`：`asset_links` 增量追加/同 alias 重指向/同 id 改名，
  revision+1 + CAS，旧 `assets` 不动。
- `H3MediaToLatent` 末尾追加可选 `任务ID` 输入（旧工作流控件顺序不变）：
  填了即认领（空输入沿用任务参数，四步进度→job，取消/中断同步回表），
  空=旧行为；主节点 `_execute_transcode_only` 每段 ds 任务配 job（失败只记行）。
- `web/h3_api.js` 新增 `transcodeSubmit/Jobs/Job/Cancel`、`libraryUploadJson/
  libraryUpload`（P3 整面板重做前备好口径）。单测 `tests/test_transcode_p1.py` 10 项。
- 验证：`pytest tests/` 45/45（旧 25 + P0 10 + P1 10）。执行期（VAE 实码）待 Comfy 内验。

## 12. P2 执行解耦（`nodes.py` / `asset_store.py`）

- 解码抽取：`_load_input_image/video/audio` 拆出 `_decode_*_file(abs_path)`，
  寻址与解码分离，兼容路径行为逐字节不变。
- 注册表：组装期 `_asset_registry()`（`_project_root_early` 自定义名即带项目
  `asset_links`，按根缓存）三源汇合 pool→legacy + 项目链接 + 全局库；
  refs 四形态（旧 label / alias / asset_id / `{asset,use}`）归一后走
  `compile_refs` 校验核，未知/超限报错文案与旧链逐字兼容
  （`引用了未知素材标签…可用标签` / `超过官方单段上限…请在段卡片少勾几个`）；
  `seg_label_orders` 仍为 `[(kind, label)]`，下游哈希/重跑/二采零改动。
  `[[标签]]` 未知条目提前到校验核报错（旧为组装期 `_apply_label_tokens` 报错，
  同为执行前 ValueError）。
- 加载期 `_ensure_pool_tensors` 三级寻址：pool 文件名旧路径 → 注册表
  `resolve_absolute`（项目内旧路径 → 全局库绝对路径 → None 回落旧加载器），
  命中经 `_decode_*_file` 直解并记 `_store_hits`；标注锚/段尾锚同口径；
  报告池明细显示真实来源 + `全局库×N` 汇总行（不进指纹）。
- 画布回退加 `_store_only` 守卫：store 解析引用存在时不再被无图池回退劫持；
  `_pool_skipped` 钳零（id 引用可使引用数超 pool 数）。
- 四组 autogrow 参考输入 tooltip 标 `【旧链兼容】`（功能不变，仅标注去向）。
- 转码直连：`_execute_transcode_only` 收尾排空后台队列同项目任务
  （`claim(proj or None)`，取消/中断/完成/失败全同步回 job 表）。
- 单测 `tests/test_exec_p2.py` 12 项：三级寻址顺序、注册表、`ref_key`、
  新核 vs 旧 `check_segment_refs` 等价（ok/未知/超限）、id 引用、nodes 接线断言。
- 验证：`pytest tests/` 57/57。已知边界：存档目录为空的指纹自动命名链若用
  项目链接 id 引用会报未知（组装期根未知带不上链接；label 链不受影响，
  导演台恒写存档目录不触发）。执行期 VAE 实码待 Comfy 内验。

## 13. P3 前端三库重做（`web/` + 链接路由 + 注册表合并）

- 后端：`build_registry` 同 id 多条目合并（全局记录首胜，别名/文件回填，
  label/alias/id 三者任一可查）；`projects.unlink_asset`（幂等解链 + CAS）；
  新路由 `GET /h3chain/asset_links`（链接表 + 全局回填，直供前端池合并）、
  `POST /h3chain/asset_link|unlink|mirror`（写链/解链/调入项目 `assets/` 拷贝，
  只拷不写 manifest）、`GET /h3chain/library_file`（全局文件直读，realpath
  禁闭，`FileResponse` 按扩展名 Content-Type）；`check_files` 认
  `images|videos|audios` 全局前缀（校验按钮不再对链接误报缺文件）。
- 执行：ds 活池 `asset_id` 进注册表（`pool_ids` 随去重对齐，未保存的新链接
  也能跑；`ref_images` 旧分支补空对齐）。
- 前端（旧字符串零丢失，见 `tests/test_frontend_p3.py` 断言）：
  `getDs` 透传 `asset_id` + refs 白名单含 id；`persistPool`/保存按钮剥离链接
  条目（住 `asset_links`）；删卡同步 `assetUnlink`、改名同步 `assetLink`；
  切项目合并 `assetLinks`（冲突链接胜出，与后端同口径）。
- 资产库面板：列表行 → 瓦片网格（缩略图/视频预告/音频条 + 别名 + 来源 +
  段引用徽标，点徽标跳段；`compileRefs` 整包干跑一次把真实 token 写回徽标
  `段N·P/V/Ak`，失败静默）；拖放区 + `＋` 按钮走 `uploadLibFile` 单次往返
  （全局库 + 本项目链接，旧后端回落两步走）；全局条目 `⇩调入项目` 进
  `assets/` 后剪辑/转码；`h3_assets.js` 新增 `segUsage/compileTags/
  uploadDirect/guessKind/submitJob/pollJob/cancelJob`（旧导出保留）。
- 提示词框 `@补全`：`@` 弹池别名（種類标注），点选/回车插入 `[[别名]]`，
  只改文本走既有防抖落盘（焦点守卫安全）。
- 转码区：保留 ds 队列区，新增 `renderServerJobs`（状态/进度条/取消，
  有活动任务 3s 轮询）+ 转码子面板“提交后台任务”按钮。
- 验证：`pytest tests/` 70/70（新增 13）；`node --check` 四前端文件通过。
  待浏览器验：瓦片拖放、@ 弹窗定位、任务轮询、旧链回归。

## 14. 代码审查修复（P0-P3 横扫）

- **B1/B2（致命）**：`_ensure_pool_tensors` 与段尾锚现加载的 store 解析只在
  `_fn is None` 时走——全局条目 `file=images/…` 在 `pool_file_of` 里有值，
  永远掉进旧 input 路径报缺文件。改为注册表优先、旧路径回落
  （legacy 条目解出来是同一字节，等价）。
- **B3**：`cleanLabel` 截 12 vs 后端 24——长别名在 widget 读写循环被截断，
  与服务端链接别名对不上。改 24。
- **B4（丢数据）**：链接条目 `首帧图/尾帧图` 标注只活 widget，切项目即丢。
  `asset_links` 加 roles；`link_asset(roles=)` 三态（None 不动 / 列表覆盖含清零）；
  改标/改名/切项目全链同步。
- **B5**：`syncMirrors` 跳过全局条目（LoadImage 只认 input，写 `images/…`
  会挂红；执行期走 store 寻址不受影响）。
- **B7/B8**：总提示词粘贴过滤与优化器媒体查找兼容 asset_id；优化器预览传直达地址。
- **B9**：multipart 中转名加唯一后缀（同名并发互踩）。
- 次要：`segUsage` id→别名归一；删死变量 `tileSubs`。
- 验证：`pytest tests/` 79/79（新增 `tests/test_review_fixes.py` 9 项）；
  `node --check` 全过。剩余待 Comfy 内验：执行期 VAE 实码、浏览器交互。

## 15. 热修复：library_upload multipart 双读 body（2026-09-10）

- 现象：瓦片拖放/＋上传报「multipart 解析失败：Could not find starting boundary」。
- 根因：handler 先 `await request.json()` 再 `await request.multipart()`；
  aiohttp 的 body 流只能消费一次，`multipart()` 拿到的是空流。
  （此前 stub 测试复现不了——桩不模拟流消费。）
- 修复：按 `Content-Type` 先分支，multipart 路径永不碰 `json()`；
  新增真 multipart 桩测试（含 `json_calls == 0` 回归断言）。
- 全 handler 扫描确认无第二个 double-read。
- 验证：`pytest tests/` 80/80。

## 16. 提交即执行（转码不再多按一次运行）

- 问题：后台任务只提交不执行，必须另凑 ds 任务再按一次运行。
- 后端：`transcode_queue.has_queued(project)`；主节点入口
  `if _jobs or _server_pending`（本项目后台表有排队即进转码专跑，
  `存档目录` 为空时保持旧行为）；专跑报告行改为
  “共完成 N 个（面板 M＋后台 K）”。
- 前端：转码子面板两按钮提交成功后 `await app.queuePrompt()` 自动排队
  （失败回落手动提示）；相关“按开始生成”文案同步。
- 语义：转码专跑只跑转码不跑链——按转码 = 立刻转码，不会顺手触发
  长生成；生成仍由用户手动排队。表在进程内存，重启丢表不丢产物。
- 验证：`pytest tests/` 89/89。

## 17. P4 冻结工作流（`bundle.py` + 两工作流改写）

- 新增 `H3AssetBundle`（`bundle.py`，注册进 `__init__.get_node_list`）：
  项目名进 → 规范包 JSON 出（链接优先 + 本地补位，`build_bundle_pack`
  纯函数可测；缺文件排队前早爆，与 Hub 同口径）。项目名空 = 空包走旧路径。
- 两工作流（配套模板 + 备用初始化）脚本化改写并全量校验：
  删 21 个镜像加载节点（LoadImage×12/LoadVideo×3/GetVideoComponents×3/
  LoadAudio×3）与 24 条连线；主节点 inputs 被删连线置空、末尾追加
  `资产包(STRING)`；新增 Bundle 节点（id 55，项目名空）单线直连；
  说明便签改单线版。主节点 29 控件原样不动。
- 主节点「资产包」tooltip 改 Bundle 优先（Hub 兼容）；README 参考素材节改单线版。
- 验证：`pytest tests/` 88/88（新增 `tests/test_workflows_frozen.py` 8 项：
  组装纯函数 + 双文件结构 + 注册/tooltip）；`node --check` 通过。
  待 Comfy 内验：模板加载/连线显示、Bundle 执行、旧链回归。

## 18. P4b 全冻结 + 模板报错排查

- 提示词组外联（PrimitiveStringMultiline×3 + 连线 7/8/9）一并拆除，
  主节点对应槽位置空；两模板现仅 8 节点
  （UNET/CLIP/双VAE/主节点/Bundle/说明/注意力/报告预览），画布零外联，
  提示词与素材全走导演台状态 + Bundle。
- 模板加载报 10 错（9 控件值 + 提示词_0 未连接）经排查与改写无关：
  `git diff` 证实 29 控件值逐字未动，静态审计确认 29 值与当前
  `nodes.py` schema 逐项对齐。根因为运行时的后端代码不是最新
  （未重启 / custom_nodes 有重复旧插件目录 / 文件没同步全），
  自查：节点菜单搜“H3 Asset Bundle”，搜不到即实锤；看控制台插件加载路径。
- 验证：`pytest tests/` 89/89。

## 19. P4c 主节点删参考拉线输入

- `define_schema` 删除四组 autogrow（参考图片/视频/音轨/音频）；
  socket 型输入不占控件槽位，29 控件值零移位。执行签名保留 `=None`
  默认，旧链回退分支原样保留（旧工作流残留连线加载时自动忽略）。
- 两工作流主节点 inputs 同步清 18 条残留条目；补救过一次槽位号：
  资产包连线 to_slot 31→13（单测的槽位全校验抓到，drop 脚本自带校验漏了该项）。
- 工具节点（Hub/现抽/库转/放大）均为“连了才生效”的按需工具，
  默认流（资产 Bundle + 转码专跑）已覆盖，不进默认工作流。
- 验证：`pytest tests/` 90/90。

## 20. P4d 主节点删画布入口与备用节点/输出

- `define_schema` 删除：首帧/尾帧/尾锚图片、提示词组 autogrow（socket 型，
  不占控件槽位，29 控件值零移位；`生成模式` 等 widget 占位只折叠不删，
  删 widget 会错位老工作流值并丢导演台 JSON）；起始视频（序章）是唯一保留的
  画布媒体入口（无导演台等价字段）；执行签名同步精简，`_canvas_fallback`
  死分支与 `_autogrow_items` 一并删除。
- 输出删除：分段图像/分段音频（分段落盘由自动保存完成）；returns 改 4 元。
- 节点删除：`H3AssetHub`（被 Bundle 取代）、`H3MediaToLatent`
  （被自动专跑取代）；纯函数（`normalize_pack`/`run_transcode_job`）保留，
  路由/Bundle/转码仍在用。保留 `H3LatentExtract`（张量现抽）与
  `H3LatentUpscale`（库内放大）：功能唯一，用时手摆。
- 两工作流主节点 inputs 同步清理（资产包槽位 31→13→6，单测槽位全校验覆盖）。
- 验证：`pytest tests/` 91/91（§19 用例同步更新）。

## 21. P4e 工具节点进默认工作流 + latent 一键放大

- 默认工作流/示例追加（全部 mode=2 旁路休眠，零执行成本，空控件不触发校验）：
  `H3LatentExtract`（56，视频帧/音轨←59 拆分，双 VAE←3/4）、
  `H3LatentUpscale`（57，模型←1，文本编码器←2，二次采样默认关闭）、
  `LoadVideo` 现抽视频源（58）→ `GetVideoComponents` 现抽拆分（59）；
  存量 VAE/MODEL/CLIP 输出扇出不断原链；槽号按文件下标。
- 导演台 latent 行「⚡放大」：写 Upscale 控件（项目名/源文件/
  保存名 `*_up2x.pt`，放大模型为空拒绝）→ 主节点临时旁路 →
  `queuePrompt` → finally 还原双方 mode（沿用 doUpscaleSeg 临时状态模式）。
  文件转 latent 仍走转码提交（无需现抽节点）。
- 验证：`pytest tests/` 93/93。

## 22. P4f 转码最小图（只加载 VAE）

- 现象：24 帧转码却加载 TE（15GB）+ DiT——因搭整图进队列。
- 后端：主节点模型/文本编码器改 `optional`（模板照连不受影响）；
  `execute` 转发处补缺省（不动签名，避免语法与兼容坑）；
  新增 `GET /h3chain/vae_files`（models/vae 列表）。
- 前端：提交后不再 `queuePrompt` 整图，改投 3 节点最小图
  （双 VAELoader + 主节点短路）到官方 `/prompt`；VAE 文件名优先读
  画布 VAELoader（与链同款），回落服务端扫描配对；失败回落整图排队。
- 验证：`pytest tests/` 95/95。

## 23. 最小图被拒 + 有声音频转码越界（2026-09-10 实测日志）

- 最小图 `prompt_no_outputs` 被拒：官方要求 prompt 至少含一个
  `OUTPUT_NODE=True` 节点（存盘/预览类），3 干活节点无出口。
  修法：最小图末尾加 `PreviewAny` 接主节点报告槽（输出 3），
  与配套模板自带 PreviewAny 同口径；回落整图是兜底。
- 有声音频 `IndexError: tuple index out of range`：`decode_av` 给 2D
  `[C,T]`，`_encode_audio_latent` 按官方 3D `[1,C,L]` 写的，
  `[:1]` 切到声道致 VAE 内越界（静音文件走零分支故此前未爆）。
  修法：函数入口补 batch 维（3D 零改动）；失败堆栈打控制台
  （报告仍只留一行）。
- 验证：`pytest tests/` 96/96（含 AST 抽取真跑 1D/2D/3D 波形）。

## 24. 最小图全控件 + 原名透传（2026-09-10 实测第二轮）

- 最小图 `prompt_outputs_failed_validation`：官方要求**所有 widget
  输入带值**（有 schema 默认值也不行，缺一即 Required input is missing；
  MODEL/CLIP 因已标 optional 得以省略）。合成图补全 29 控件
  （默认+存档目录/导演台状态/资产包覆盖），排队错误详情透出到提示。
- 中转名污染：multipart 随机中转名漏进 `orig_name`，调入/转码起名
  全带 `h3lib_` 前缀。`register_content` 加 `orig_name` 参数，
  上传分支透传真实文件名（缺省行为不变）。
- 验证：`pytest tests/` 97/97。

## 25. 转码慢诊断（6GB 显存墙，2026-09-10 实测）

- 现象：24 帧转码 11 分钟未完；日志证实最小图已生效（无 TE/DiT，
  仅 2 VAE），慢在 `MiniMaxH3VideoVAE 5309MB Staged` fp32 逐层串流。
- 本轮只加诊断不改行为：转码前打印输入形状/设备/VAE 精度/显存；
  显存 <8GB + fp32 即提示加 `--fp16-vae`（VAE 减半，全链同对象）。
  不在本插件内改精度（与主链 latent 空间必须一致）。
- 验证：`pytest tests/` 98/98。

## 26. 流式剪辑 + 面板 hydration + 全屏三库（2026-09-10 实测驱动）

- 分离/裁剪 OOM（19GB 整片解码卡死整机）：`trim_av_mp4` 改 seek+窗口
  流式转码（内存 O(在途帧)，行为对齐旧版：从 0 计时/源帧率/crf 档/
  空 aac 轨/.part 原子）；新增 `split_av_stream`（视频 packet 级
  remux 零解码 + 音频流式收 PCM）与 `save_wav_pcm16`；
  `projects.split_av` 只换心脏（校验/命名/登记不动）；音频 1GB 熔断。
  另修工作区 media.py `\r\r\n` 脏行尾（ pristine 重打）。
- 资产库首次空白：面板只读 widget 旧池。`paintBody` 绘制前
  `mergeServerPool` 回填（widget 优先、asset_id 对齐、链接覆盖同名）。
- 全屏三库：`.h3d-libbox` 真全屏 + 库内大按钮；资产库拆
  全局/项目 × 图/视/音六分栏；各区 `<details>` 伸缩框（折叠态常驻内存，
  重绘不丢）；成片/latent 分组、左栏项目列表同口径折叠。
- 验证：`pytest tests/` 109/109（含 fake-av 流式计数：窗口解码上界、
  分离视频零解码、wav 回环、端到端 manifest）。

## 27. 显性软引用语义（2026-09-10 确认）

- 约定：绑定 = 媒体注入 + tag 保证落进最终提示词；英文散文长文不再自动写。
- 后端：`_reference_header`（英文 subject_definitions）删除，换
  `_reference_tags_minimal`（`[References]` + 每行 `token = 别名`）；
  只补正文缺的 tag（已写直通）；`_uncovered_tags` 精确匹配
  （`<Picture 1>` 是 `<Picture 10>` 子串陷阱，回归覆盖）。
- 前端：v2 调度勾选即在段正文补可见 `[[标签]]`（取消不删）；
  统一引用条/瓦片引用/@ 早已如此；瓦片徽标显示真实 tag。
- 文档：README r2v 条 + 总提示词 skill 契约同步为最小映射。
- 验证：`pytest tests/` 116/116（含 AST 真跑装配内核）。

## 28. 切存档竞态修复（2026-09-10 多次复现）

- 跨项目污染：350ms 输入防抖在切项目后到期，把旧按键写进新项目。
  `debounce*Write` 登记可 flush 闭包（`_taPending`）+ 代际号；
  `switchProject` 先同步刷进旧项目（一个不丢），过期写回直接丢弃。
- 快切互盖：连续点两个项目，慢的回包覆盖新的。`_uiGen` 代际守卫，
  每次 await 后验旧即退。
- 回写失败中断切换：`flushPrompts` 加 try/catch（网络抖动不再卡死切换）。
- 空项目残留：manifest 无段落时旧分支直接跳过，旧池/旧词残留显示。
  加 else 分支清空（prompts/segments/inserts/ref_assets）。
- 槽位态泄漏：`redo_segs` / `upscale.include` 按槽位存，切项目不清会
  在新项目重做错段——两分支落盘前清零。
- 感知延迟：切换结尾直接 `await refresh()` 全刷，不等 200ms 轮询。
- 验证：`pytest tests/` 117/117。
