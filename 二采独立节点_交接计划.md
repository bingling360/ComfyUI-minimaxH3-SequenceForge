# 二采独立节点 · 本轮修改总结 + 交接计划

> 用途：本轮（2026-09-01 晚）只修了一个注册 bug，已可用。但独立二采节点与导演台/主节点的「关系」仍悬而未决——这是用户觉得「不对劲」的根源。本文档给下个 AI 接手。
> 当前分支：experiment ｜ 目标：打包分发 ｜ ComfyUI 版本：统一 0.34.2（G 机 0.33.1 无 comfy_api.latest，跑不了本插件）。

---

## 一、本轮修改总结（已交付，无需再改代码）

### 现象
用户报告：旧节点 `H3SeamlessChainSampler` / `H3SeamDoctor` 正常，但新加的 **二采·归 `UpscaleNode`** 和 **二采·取 `H3LatentLoadSegment`** 在 ComfyUI 节点列表里找不到。

### 根因
- 旧节点是 **V3 节点**：`class H3SeamlessChainSampler(io.ComfyNode)` + `define_schema()`，**自带 `GET_SCHEMA()`**。
- 新节点当初写成**经典节点**：`INPUT_TYPES` + `RETURN_TYPES` + `FUNCTION`，不继承 `io.ComfyNode`，**没有 `GET_SCHEMA()`**。
- 本插件加载走 V3 路（`__init__.py` 的 `comfy_entrypoint`）。加载器 `ComfyUI/nodes.py:2292` 是 `if NODE_CLASS_MAPPINGS elif comfy_entrypoint`（互斥）；V3 路对 `get_node_list()` 返回的**每个**类调 `node_cls.GET_SCHEMA()`（`nodes.py:2320-2321`）。
- 新节点缺 `GET_SCHEMA` → `AttributeError` → 被 `nodes.py:2328` 的 `except` 整模块跳过。旧节点在报错前已注册完，所以**旧节点在、新节点被静默丢弃**。

### 修复
把 `upscale_node.py` 里两个新节点改写为 **V3 `io.ComfyNode`**（与旧节点同构）：
- `define_schema()` 返回 `io.Schema(node_id="UpscaleNode"/"H3LatentLoadSegment", display_name=…, category="H3/二采", inputs=[io.X.Input(...)], outputs=[...])`；
- `execute` 从实例/`FUNCTION` 改为 `@classmethod`；
- 引擎逻辑不变：仍委托 `upscale.render_segment`（取/归共用同一引擎，零行为漂移）。
- `__init__.py` **未改**（本来就返回这 4 个节点）。

### 验证
本机无 torch，用桩模块（假 `torch` + 假 `comfy_api.latest.io`）导入**真实 `upscale_node.py`** 实调 `GET_SCHEMA()`：两节点均成功、schema 类型校验通过、`node_id` 与示例工作流 `class_type` 一致、`execute` 参数名/顺序与 `define_schema` 输入逐一对齐（`ALL_GET_SCHEMA_OK`）；`py_compile` 通过；已同步到本地 `custom_nodes` 镜像。用户确认「现在可以用了」。

### 改动文件
- `upscale_node.py`（~530 行，V3 化；workspace + 本地 custom_nodes 镜像均已更新）
- 示例工作流 `example_workflows/二采独立节点示例.json` **未改**（class_type 不变，schema 输入顺序与经典 INPUT_TYPES 一致，widgets_values 不位移）。

---

## 二、当前架构关系（事实，下个 AI 先读这块）

```
导演台前端 web/h3_director.js
   │  写入「导演台状态」JSON（模式/提示词/素材/分段处理）
   ▼
主节点 H3SeamlessChainSampler (io.ComfyNode, V3)
   │  - 多段全链生成
   │  - 内联二采：主循环直接调 upscale.render_segment（每段生成完自动高清放大重采样）
   │  - 导演台「🔄 重新二采此段」按钮 → doUpscaleSeg → _up_hi → render_segment（走主链，不走 UpscaleNode）
   ▼
二采引擎 upscale.py：render_segment / render_latent / load_net（真实通道，单测+G 机实测）

新增（并行、孤儿链路，未接导演台）：
H3LatentLoadSegment（二采·取）──读 <root>/seg_NNN.pt（torch 私有 pickle）→ 基础段 video/audio latent + 可选上段尾桥锚
        │
        ▼
UpscaleNode（二采·归）── 放大网络超分 → 高清 cond 注入桥锚(CondSync) → 低强度重采样 → 解码 → 覆盖 seg_NNN.mp4 + 写 manifest
   - 需手动接 model/clip/vae/audio_vae（见示例工作流：UNETLoader+CLIPLoader+双VAELoader+ModelAttentionBackend+CLIPTextEncode）
   - 委托同一引擎 upscale.render_segment
```

**关键事实**：
1. 主链内联二采 与 UpscaleNode 都调 `upscale.render_segment` → 理论零行为漂移，但**参数面板有两套**（导演台二采面板 vs UpscaleNode INPUT_TYPES），改一处另一处不同步。
2. 导演台「重新二采此段」按钮**当前不接** UpscaleNode，走主链 `_up_hi`。独立节点是图里手动直连的备用入口。
3. 桥锚协议一致：`UpscaleNode._guide_of` 用 0 帧哨兵识别「无桥锚」，`H3LatentLoadSegment` 在无桥锚时输出 0 帧空哨兵；`ctx` 取 5/22/39/56，需与主链「引导帧数」一致。
4. 存档格式耦合：seg_NNN.pt 是 H3 私有 `torch.save({"video","audio"})`，核心 Load Latent 读不了 → 必须有「取」节点；归节点同名覆盖。

---

## 三、用户觉得「不对劲」的点 = 下个 AI 要决策/处理的开放问题

1. **双二采路径会不会语义漂移**：主链内联二采 vs UpscaleNode 共用引擎，但目前是「两个入口两套面板」。若长期并存，参数改动容易只改一边。
2. **导演台没接独立节点**：按钮仍走主链，「二采·归/取」是孤儿，用户得手搓图。要么把按钮改接 UpscaleNode，要么明确「独立节点=手动备用」并写进文档。
3. **UpscaleNode 需手接 model/clip/vae**：导演台场景里这些由主节点内部加载；若要让导演台一键触发单段二采，需给 UpscaleNode 加 `导演台状态` JSON 输入（参考主节点 `socketless=True`）或新增 API 路由由导演台 POST 触发。
4. **桥锚/取 与主链存档约定一致性**：独立节点读主链产出的 seg_NNN.pt，依赖「主链已经生成过该段」。需确认 ctx/引导帧数/高清 manifest 字段在两条路径下完全对齐，否则高清接缝会和主链不一致。
5. **路由缺口**：插件路由（PromptServer，/h3chain/*）是给导演台用的；独立节点是否要新路由（如「触发某段二采」的 API）未定。

---

## 四、建议后续任务清单（下个 AI 认领）

- [ ] **决策 A**：独立节点定位——「官方二采入口」还是「手动备用」？据此决定导演台按钮是否改接 UpscaleNode。
- [ ] **决策 B**：若接导演台，选方案——
  - (B1) 给 UpscaleNode 加 `导演台状态` JSON 输入（同主节点 socketless），导演台按钮拼好 JSON POST；或
  - (B2) 新增 `/h3chain/upscale_seg` 路由，导演台按钮调用，后端起 UpscaleNode 同样委托 render_segment。
- [ ] **统一二采参数源**：把导演台二采面板与 UpscaleNode INPUT_TYPES 对齐到同一 schema（`upscale.parse_state` v2），消除双份面板漂移风险。
- [ ] **桥锚一致性回归**：跑一段已知项目，对比「主链内联二采」vs「UpscaleNode 二采」产物，确认高清接缝/段2 形状无差异。
- [ ] **文档**：README 写「二采两种用法」（主链内联 / 独立节点）含示例工作流链接；说明何时用哪个。
- [ ] **示例工作流增强**（可选）：补一条「导演台驱动」或「接主链输出」的链路示例，减少用户手搓。

---

## 五、关键文件 & 约定速查（下个 AI 必读）

| 文件 | 角色 |
|---|---|
| `upscale_node.py` | V3 独立节点 `UpscaleNode`(归) + `H3LatentLoadSegment`(取)；`define_schema`+`execute`，委托 `upscale.render_segment` |
| `nodes.py` | 主节点 `H3SeamlessChainSampler(io.ComfyNode)`；`_tail_keyframe`（桥锚切片）；`full_bridge_supported()` |
| `upscale.py` | 二采引擎 `render_segment`/`render_latent`/`load_net`；`_apply_guide`/`CondSync` |
| `guides.py` | `audit_keyframes`（桥锚越界体检） |
| `checkpoint.py` | `seg_NNN.pt` 读写（`save_segment`/`load_segment`） |
| `web/h3_director.js` | 导演台前端；「🔄 重新二采此段」按钮 |
| `routes.py` | 插件 API 路由（PromptServer / 新版 add_routes） |
| `example_workflows/二采独立节点示例.json` | 取→归 手动链路示例 |

**铁律（本项目）**：本插件是**纯 V3 插件**（旧节点已依赖 `io.ComfyNode`）。新增节点**必须**写成 `io.ComfyNode` 子类 + `define_schema()`，否则 `get_node_list` 返回的类缺 `GET_SCHEMA()`，加载器会**静默丢弃整个插件的所有节点**。经典 `INPUT_TYPES` 节点塞进本插件的 `comfy_entrypoint` 路必炸。

**版本**：统一 ComfyUI 0.34.2；G 机 0.33.1 无 `comfy_api.latest`，跑不了本插件（旧节点也跑不了）。
